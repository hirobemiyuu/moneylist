<% if @users.any? %>
  <ul class="list-unstyled">
    <% @users.each do |user| %>
    <li>
      <div class="body">
        <div>
          <p><%= link_to user.name, user_path(user) %>'s page</p>
        </div>
      </div>
    </li>
    <% end %>
  </ul>
  <%= paginate @users %>
<% end %>