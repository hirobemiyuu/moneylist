<% if @users.any? %>
  <ul class="list-unstyled">
    <% @users.each do |user| %>
    <li>
      <div class="body">
        <div>
          ・
          <img src="<%= user.image.url %>" width="50", height="50">
          <%= link_to user.name, user_path(user) %>
        </div>
      </div>
    </li>
    <li><p></p></li>
    <% end %>
  </ul>
  <%= paginate @users %>
<% end %>